import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    tab: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    tabContainer: {
        flexDirection: 'row',
        height: 66,
        backgroundColor: "#fff",
        borderWidth: 2,
        borderColor: "grey",
    },
    tabIcon: { marginRight: 4, marginBottom:3 },
});
